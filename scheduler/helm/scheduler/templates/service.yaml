apiVersion: v1
kind: Service
metadata:
  name: scheduler
spec:
  selector:
    app: scheduler

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000

  type: {{ .Values.service.type }}