replicaCount: 1

image:
  repository: scheduler
  tag: latest

service:
  type: ClusterIP
  port: 8000

ingress:
  enabled: true
  host: scheduler.lab.local

resources:
  requests:
    cpu: "200m"
    memory: "256Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

env:
  LOG_LEVEL: INFO