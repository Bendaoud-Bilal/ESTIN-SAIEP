apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor-sandbox
# Points to the containerd handler configuration. 
# Defaults to runc behavior if runsc is not yet registered on the VPS host.
handler: runsc